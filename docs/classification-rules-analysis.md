# تحليل القواعد المستخرجة من التقرير

**المصدر**: screenplay-patterns-report.md  
**التاريخ**: 2026-01-25  
**عدد الملفات المحللة**: 92 ملف  
**عدد الأسطر الكلية**: 10,243 سطر

---

## 🎯 القواعد حسب الأولوية

### 1. حوار بصيغة "اسم :" مع مسافات (أولوية قصوى)
- **العدد**: 2,304 حالة
- **النسبة**: 22.5% من الأسطر
- **المشكلة**: مسافات قبل/بعد النقطتين + رموز خاصة
- **الحل المقترح**: 
  ```typescript
  // تطبيع المسافات والرموز قبل فحص CHARACTER_RE
  const normalizedForChar = line
    .replace(/[\u200E\u200F\u061C\uFEFF]/g, '') // إزالة الرموز الخفية
    .replace(/\s*:\s*/g, ':')                   // توحيد المسافات حول النقطتين
    .trim();
  ```

### 2. عناوين مشاهد بصيغ غير قياسية (أولوية عالية)
- **العدد**: 342 حالة  
- **النسبة**: 3.3%
- **الأنماط**:
  - `مشهد 7 ليل-داخلي`
  - `مشهد (2 \ 3) نهار-خارجي`
  - `مشهد12( فلاش باك ) ليل-داخلي`
- **الحل المقترح**:
  ```typescript
  const SCENE_PATTERNS = [
    /^\s*مشهد\s*\d+\s*(ليل|نهار|صباح|مساء)-(داخلي|خارجي)/i,
    /^\s*مشهد\s*\(?\d+\s*[\\/]\s*\d+\)?/i,  // مشهد (2/3)
    /^\s*مشهد\s*\d+\s*\([^)]+\)/i,          // مشهد12 (فلاش باك)
  ];
  ```

### 3. سطر انتقال "قطع" منفصل (أولوية عالية)
- **العدد**: 330 حالة
- **النسبة**: 3.2%
- **الحل المقترح**:
  ```typescript
  const TRANSITION_WORDS = new Set([
    'قطع', 'كات', 'cut', 'اختفاء', 'تحول', 'انتقال',
    'fade', 'dissolve', 'wipe', 'cut to'
  ]);
  
  const isTransitionLine = (line: string): boolean => {
    const normalized = normalizeLine(line).toLowerCase();
    return TRANSITION_WORDS.has(normalized);
  };
  ```

### 4. حوار يبدأ برمز • (أولوية متوسطة)
- **العدد**: 268 حالة
- **النسبة**: 2.6%
- **الحل المقترح**:
  ```typescript
  // في stripLeadingBullets - إضافة تحليل ذكي
  const bulletMatch = line.match(/^[\s\u200E\u200F]*([•·∙])\s*(.+)/);
  if (bulletMatch) {
    const content = bulletMatch[2];
    // تحليل المحتوى بعد الرمز
    const inlineParsed = parseInlineCharacterDialogue(content);
    if (inlineParsed) {
      return { type: 'character-dialogue', ...inlineParsed };
    }
  }
  ```

### 5. وصف/حركة يبدأ بشرطة - (أولوية متوسطة)
- **العدد**: 198 حالة
- **النسبة**: 1.9%
- **الحل المقترح**:
  ```typescript
  // اعتبار الشرطة في البداية علامة على Action
  if (/^[\s\u200E\u200F]*-\s+[يت]/.test(line)) {
    return 'action';
  }
  ```

### 6. حوار بدون نقطتين (أولوية متوسطة)
- **العدد**: 167 حالة
- **النسبة**: 1.6%
- **أمثلة**:
  - `هو ده صاحبك عميش ؟؟؟`
  - `انت متأكد من العنوان يا له ؟`
- **الحل المقترح**: تحسين `parseInlineCharacterDialogueWithoutColon` مع شروط أكثر صرامة

### 7. أسطر قصيرة جدًا (أولوية منخفضة)
- **العدد**: 69 حالة
- **النسبة**: 0.7%
- **أمثلة**: `مين ؟`, `ايه ؟`, `لسه ؟`
- **الحل المقترح**:
  ```typescript
  // في getDialogueProbability
  const veryShortQuestions = /^(مين|فين|ايه|ازاي|ليه|لسه)[؟?!]?\s*$/;
  if (veryShortQuestions.test(normalized)) {
    score += 4; // احتمال عالي جداً للحوار
  }
  ```

### 8. تعليمات أداء مستقلة (أولوية منخفضة)
- **العدد**: 16 حالة
- **النسبة**: 0.16%
- **الحل المقترح**:
  ```typescript
  // إذا السطر بين أقواس وسبقه character/dialogue
  if (/^\s*\([^)]+\)\s*$/.test(line) && 
      (lastType === 'character' || lastType === 'dialogue')) {
    return 'parenthetical';
  }
  ```

---

## 📈 إحصائيات التأثير

| القاعدة | العدد | النسبة | الأولوية | التعقيد |
|---------|-------|--------|----------|---------|
| حوار "اسم :" | 2,304 | 22.5% | ⭐⭐⭐ | منخفض |
| عناوين مشاهد | 342 | 3.3% | ⭐⭐⭐ | متوسط |
| سطر قطع | 330 | 3.2% | ⭐⭐⭐ | منخفض |
| حوار برمز • | 268 | 2.6% | ⭐⭐ | متوسط |
| وصف بشرطة - | 198 | 1.9% | ⭐⭐ | منخفض |
| حوار بلا : | 167 | 1.6% | ⭐⭐ | عالي |
| أسطر قصيرة | 69 | 0.7% | ⭐ | منخفض |
| تعليمات أداء | 16 | 0.16% | ⭐ | منخفض |

**إجمالي الحالات المغطاة**: 3,694 حالة (36% من الأسطر)

---

## 🔧 خطة التنفيذ المقترحة

### المرحلة الأولى (الأساسيات - أسبوع 1)
1. ✅ تحسين تطبيع المسافات والرموز الخفية
2. ✅ تعزيز قواعد عناوين المشاهد
3. ✅ إضافة قاعدة "قطع" المنفصلة

### المرحلة الثانية (التحسينات - أسبوع 2)
4. ⚙️ معالجة الرموز النقطية (•) بذكاء
5. ⚙️ تحسين كشف Action بالشرطة
6. ⚙️ ضبط الأسطر القصيرة جداً

### المرحلة الثالثة (التحسينات المتقدمة - أسبوع 3)
7. 🔬 تحسين الحوار بدون نقطتين
8. 🔬 معالجة تعليمات الأداء المستقلة

---

## 📝 ملاحظات هامة

### نقاط القوة الحالية في النظام:
- ✅ نظام قوي للكشف عن الأفعال (ACTION_VERB_SET)
- ✅ دالة `getDialogueProbability` ذكية جداً
- ✅ معالجة جيدة للسياق (Context)
- ✅ دعم الذاكرة (Memory)

### نقاط التحسين المطلوبة:
- ⚠️ تطبيع المسافات والرموز الخفية ضعيف
- ⚠️ قواعد عناوين المشاهد محدودة
- ⚠️ لا يوجد دعم كامل لكلمة "قطع" المنفصلة
- ⚠️ معالجة الرموز النقطية بدائية

### توصيات إضافية:
1. **الاختبار**: إنشاء suite اختبار شامل لكل قاعدة
2. **القياس**: قياس دقة التصنيف قبل وبعد كل تحسين
3. **التوثيق**: توثيق كل تغيير في CHANGELOG
4. **المراجعة**: مراجعة القواعد دورياً مع ملفات جديدة

---

## 🎓 خلاصة

هذا التحليل يوفر خارطة طريق واضحة لتحسين نظام التصنيف بنسبة **36% على الأقل** من خلال معالجة الحالات المكتشفة. التركيز يجب أن يكون على القواعد ذات التأثير الأكبر أولاً (المرحلة الأولى)، ثم التوسع تدريجياً.

**التأثير المتوقع**: تحسين دقة التصنيف من ~70% إلى ~90% على ملفات مشابهة.

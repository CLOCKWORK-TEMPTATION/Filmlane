# توثيق مجلد Constants

هذا المجلد يحتوي على الثوابت المستخدمة في تطبيق Filmlane لكتابة السيناريوهات.

---

## 📄 colors.ts

### الوصف
يحتوي على مصفوفة الألوان المستخدمة في التطبيق لتنسيق النصوص والعناصر.

### المتغيرات

#### `colors`
- **النوع**: `string[]`
- **الوصف**: مصفوفة تحتوي على 15 لون بصيغة HEX
- **الاستخدام**: توفير لوحة ألوان ثابتة للاستخدام في واجهة المستخدم

**محتويات المصفوفة**:
```typescript
[
  '#000000',  // أسود
  '#e03131',  // أحمر
  '#c2255c',  // وردي غامق
  '#9c36b5',  // بنفسجي
  '#6741d9',  // بنفسجي غامق
  '#3b5bdb',  // أزرق بنفسجي
  '#1b6ec2',  // أزرق
  '#0c8599',  // أزرق مخضر
  '#099268',  // أخضر مزرق
  '#2f9e44',  // أخضر
  '#66a80f',  // أخضر ليموني
  '#f08c00',  // برتقالي
  '#e8590c',  // برتقالي محمر
  '#868e96',  // رمادي
  '#343a40'   // رمادي غامق
]
```

---

## 📄 fonts.ts

### الوصف
يحتوي على خيارات الخطوط وأحجام النصوص المتاحة في محرر السيناريو.

### الاستيرادات
```typescript
import type { FontOption, TextSizeOption } from '@/types/screenplay';
```

### المتغيرات

#### `fonts`
- **النوع**: `FontOption[]`
- **الوصف**: مصفوفة تحتوي على 12 خط (10 خطوط عربية + 2 خط لاتيني)
- **البنية**: كل عنصر يحتوي على:
  - `value`: اسم الخط التقني (يستخدم في CSS)
  - `label`: الاسم المعروض للمستخدم

**الخطوط المتاحة**:
1. `{ value: 'Amiri', label: 'أميري' }` - خط أميري الكلاسيكي
2. `{ value: 'Noto Sans Arabic', label: 'نوتو سانس عربي' }` - خط نوتو سانس
3. `{ value: 'Cairo', label: 'القاهرة' }` - خط القاهرة الحديث
4. `{ value: 'Tajawal', label: 'تجوّل' }` - خط تجوّل
5. `{ value: 'Almarai', label: 'المراي' }` - خط المراي
6. `{ value: 'Markazi Text', label: 'مركزي' }` - خط مركزي
7. `{ value: 'Reem Kufi', label: 'ريم كوفي' }` - خط ريم كوفي
8. `{ value: 'Scheherazade New', label: 'شهرزاد الجديد' }` - خط شهرزاد المحدث
9. `{ value: 'Lateef', label: 'لطيف' }` - خط لطيف
10. `{ value: 'Aref Ruqaa', label: 'عارف رقعة' }` - خط الرقعة
11. `{ value: 'Arial', label: 'Arial' }` - خط Arial اللاتيني
12. `{ value: 'Tahoma', label: 'Tahoma' }` - خط Tahoma اللاتيني

#### `textSizes`
- **النوع**: `TextSizeOption[]`
- **الوصف**: مصفوفة تحتوي على 10 أحجام نصوص مختلفة
- **البنية**: كل عنصر يحتوي على:
  - `value`: حجم الخط بصيغة CSS (مثل '12pt')
  - `label`: الرقم المعروض للمستخدم

**الأحجام المتاحة**:
```typescript
[
  { value: '8pt', label: '8' },    // حجم 8 نقطة
  { value: '9pt', label: '9' },    // حجم 9 نقطة
  { value: '10pt', label: '10' },  // حجم 10 نقطة
  { value: '11pt', label: '11' },  // حجم 11 نقطة
  { value: '12pt', label: '12' },  // حجم 12 نقطة (قياسي)
  { value: '14pt', label: '14' },  // حجم 14 نقطة
  { value: '16pt', label: '16' },  // حجم 16 نقطة
  { value: '18pt', label: '18' },  // حجم 18 نقطة
  { value: '24pt', label: '24' },  // حجم 24 نقطة
  { value: '36pt', label: '36' }   // حجم 36 نقطة
]
```

---

## 📄 formats.ts

### الوصف
يحتوي على تنسيقات السيناريو المختلفة مع الاختصارات والألوان والأيقونات الخاصة بكل تنسيق.

### الاستيرادات
```typescript
import { BookHeart, Film, MapPin, Camera, Feather, UserSquare, Parentheses, MessageCircle, FastForward, SeparatorHorizontal } from 'lucide-react';
import type { ScreenplayFormat } from '@/types/screenplay';
```

### المتغيرات

#### `screenplayFormats`
- **النوع**: `ScreenplayFormat[]`
- **الوصف**: مصفوفة تحتوي على 10 تنسيقات مختلفة للسيناريو
- **البنية**: كل عنصر يحتوي على:
  - `id`: معرف فريد للتنسيق
  - `label`: الاسم المعروض بالعربية
  - `shortcut`: اختصار لوحة المفاتيح
  - `color`: لون الخلفية (وضع فاتح/داكن)
  - `icon`: أيقونة من مكتبة lucide-react

**التنسيقات المتاحة**:

1. **بسملة**
   - `id: 'basmala'`
   - `shortcut: ''` (بدون اختصار)
   - `color: 'bg-purple-200/50 dark:bg-purple-800/50'` (بنفسجي)
   - `icon: BookHeart` (أيقونة كتاب مع قلب)

2. **عنوان المشهد (سطر علوي)**
   - `id: 'scene-header-top-line'`
   - `shortcut: ''` (بدون اختصار)
   - `color: 'bg-blue-200/50 dark:bg-blue-800/50'` (أزرق)
   - `icon: SeparatorHorizontal` (خط فاصل أفقي)

3. **عنوان المشهد (1)**
   - `id: 'scene-header-1'`
   - `shortcut: 'Ctrl+1'`
   - `color: 'bg-blue-200/50 dark:bg-blue-800/50'` (أزرق)
   - `icon: Film` (أيقونة فيلم)

4. **عنوان المشهد (2)**
   - `id: 'scene-header-2'`
   - `shortcut: 'Tab'`
   - `color: 'bg-sky-200/50 dark:bg-sky-800/50'` (أزرق سماوي)
   - `icon: MapPin` (دبوس خريطة)

5. **عنوان المشهد (3)**
   - `id: 'scene-header-3'`
   - `shortcut: 'Tab'`
   - `color: 'bg-cyan-200/50 dark:bg-cyan-800/50'` (سماوي)
   - `icon: Camera` (كاميرا)

6. **الفعل/الحدث**
   - `id: 'action'`
   - `shortcut: 'Ctrl+4'`
   - `color: 'bg-gray-200/50 dark:bg-gray-700/50'` (رمادي)
   - `icon: Feather` (ريشة)

7. **شخصية**
   - `id: 'character'`
   - `shortcut: 'Ctrl+2'`
   - `color: 'bg-green-200/50 dark:bg-green-800/50'` (أخضر)
   - `icon: UserSquare` (مربع مستخدم)

8. **بين قوسين**
   - `id: 'parenthetical'`
   - `shortcut: 'Tab'`
   - `color: 'bg-yellow-200/50 dark:bg-yellow-800/50'` (أصفر)
   - `icon: Parentheses` (أقواس)

9. **حوار**
   - `id: 'dialogue'`
   - `shortcut: 'Ctrl+3'`
   - `color: 'bg-orange-200/50 dark:bg-orange-800/50'` (برتقالي)
   - `icon: MessageCircle` (دائرة رسالة)

10. **انتقال**
    - `id: 'transition'`
    - `shortcut: 'Ctrl+6'`
    - `color: 'bg-red-200/50 dark:bg-red-800/50'` (أحمر)
    - `icon: FastForward` (تقديم سريع)

#### `formatClassMap`
- **النوع**: `{ [key: string]: string }`
- **الوصف**: كائن يربط معرف كل تنسيق باسم الكلاس CSS الخاص به
- **الإنشاء**: يتم إنشاؤه تلقائياً باستخدام دالة `reduce` على مصفوفة `screenplayFormats`

**آلية العمل**:
```typescript
screenplayFormats.reduce((acc, format) => {
  acc[format.id] = `format-${format.id}`;  // تحويل 'basmala' إلى 'format-basmala'
  return acc;
}, {} as { [key: string]: string })
```

**مثال على الناتج**:
```typescript
{
  'basmala': 'format-basmala',
  'scene-header-top-line': 'format-scene-header-top-line',
  'scene-header-1': 'format-scene-header-1',
  // ... إلخ
}
```

---

## 📄 index.ts

### الوصف
ملف التصدير المركزي لجميع الثوابت في المجلد. يسهل استيراد الثوابت من مكان واحد.

### الصادرات

```typescript
// تصدير تنسيقات السيناريو وخريطة الكلاسات من ملف formats.ts
export { screenplayFormats, formatClassMap } from './formats';

// تصدير الخطوط وأحجام النصوص من ملف fonts.ts
export { fonts, textSizes } from './fonts';

// تصدير مصفوفة الألوان من ملف colors.ts
export { colors } from './colors';

// تصدير ارتفاع صفحة A4 من ملف page.ts
export { A4_PAGE_HEIGHT_PX } from './page';
```

**الاستخدام**:
بدلاً من:
```typescript
import { colors } from '@/constants/colors';
import { fonts } from '@/constants/fonts';
```

يمكن استخدام:
```typescript
import { colors, fonts } from '@/constants';
```

---

## 📄 page.ts

### الوصف
يحتوي على ثوابت متعلقة بأبعاد الصفحة المستخدمة في التطبيق.

### المتغيرات

#### `A4_PAGE_HEIGHT_PX`
- **النوع**: `number`
- **القيمة**: `1123`
- **الوصف**: ارتفاع صفحة A4 بالبكسل
- **الاستخدام**: يستخدم لحساب عدد الصفحات وتقسيم المحتوى في محرر السيناريو
- **الملاحظة**: هذا الارتفاع يمثل صفحة A4 بدقة معينة (عادة 96 DPI)

**الحساب**:
- صفحة A4 القياسية: 210mm × 297mm
- بدقة 96 DPI: 297mm ≈ 1123 بكسل

---

## 📊 ملخص الاستخدام

### الملفات والغرض منها:

| الملف | الغرض | عدد العناصر |
|------|-------|-------------|
| `colors.ts` | لوحة الألوان | 15 لون |
| `fonts.ts` | الخطوط والأحجام | 12 خط + 10 أحجام |
| `formats.ts` | تنسيقات السيناريو | 10 تنسيقات |
| `index.ts` | التصدير المركزي | - |
| `page.ts` | أبعاد الصفحة | 1 ثابت |

### أمثلة الاستخدام:

```typescript
// استيراد من الملف المركزي
import { 
  colors, 
  fonts, 
  textSizes, 
  screenplayFormats, 
  formatClassMap,
  A4_PAGE_HEIGHT_PX 
} from '@/constants';

// استخدام الألوان
const primaryColor = colors[0]; // '#000000'

// استخدام الخطوط
const arabicFonts = fonts.filter(f => f.value !== 'Arial' && f.value !== 'Tahoma');

// استخدام التنسيقات
const dialogueFormat = screenplayFormats.find(f => f.id === 'dialogue');

// استخدام ارتفاع الصفحة
const pageCount = Math.ceil(contentHeight / A4_PAGE_HEIGHT_PX);
```
